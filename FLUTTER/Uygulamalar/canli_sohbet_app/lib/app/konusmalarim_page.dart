import 'package:flutter/material.dart';

class Konusmalarim extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Example 1'),
      ),
      body: Center(
        child: Text('Example 1 Page'),
      ),
    );
  }
}
